# Knowledge / RAG (future phase — stub)

Anticipated seam: retrieval surfaces as a **new `Tool` implementation**
(ports/tools.py) plus new `ContentPart` types for cited chunks. The runtime,
strategies, and event model do not change. Not started.